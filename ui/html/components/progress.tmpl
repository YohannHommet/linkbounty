{{define "progress-running"}}
<div id="progress"
  hx-get="/r/{{.JobID}}/status"
  hx-trigger="every 2s"
  hx-swap="outerHTML"
  aria-live="polite"
  class="scan-wait">
  <div class="swiss-loader" aria-hidden="true"><span></span></div>
  <p class="swiss-status">Connexion au site…</p>
</div>
{{end}}
